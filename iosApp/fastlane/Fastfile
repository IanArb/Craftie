default_platform(:ios)

platform :ios do
     #1
     desc "Create dev ipa"
     #2
     lane :build_beta_dev do
     #3
     enable_automatic_code_signing
     #4
     increment_build_number(
        build_number: "1.0.0"
     )
     #5
     gym(scheme: "DEV")
  end
 end